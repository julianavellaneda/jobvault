# Screenshots

Drop the images referenced by the main README here:

- `dashboard.png` — the Dashboard view (streak, funnel, heatmap)
- `applications.png` — the Applications grid with an expanded row
- `kanban.png` — the Kanban board mid-drag

Aim for ~1280×800, light or dark theme (be consistent). A short demo GIF/MP4 of
the add-link → triage → move-across-Kanban flow converts best on launch posts —
add it as `demo.gif` and reference it in the README hero.
